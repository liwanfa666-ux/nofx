# 仓库私有化指南

本文档将指导您如何将这个公共仓库转换为私有仓库。

## ⚠️ 重要提醒

在将仓库私有化之前，请确保：

1. ✅ **备份重要数据**：克隆或下载仓库的完整副本
2. ✅ **检查敏感信息**：确认没有真实密钥、密码或敏感数据泄露
3. ✅ **通知协作者**：如果仓库有协作者，提前通知他们
4. ✅ **更新文档**：私有化后可能需要更新相关文档和链接

## 📋 私有化步骤

### 方法一：GitHub 私有化（推荐）

如果您使用的是 GitHub：

1. **登录 GitHub**
   - 访问 https://github.com
   - 登录您的账户

2. **进入仓库设置**
   - 导航到您的仓库页面
   - 点击右上角的 **"Settings"**（设置）标签

3. **更改可见性**
   - 滚动到页面底部的 **"Danger Zone"**（危险区域）
   - 点击 **"Change visibility"**（更改可见性）
   - 选择 **"Make private"**（设为私有）
   - 输入仓库名称确认

4. **完成私有化**
   - GitHub 将立即将仓库设为私有
   - 只有您和您授权的协作者可以访问

### 方法二：GitLab 私有化

如果您使用的是 GitLab：

1. **进入项目设置**
   - 导航到 **Settings** → **General**
   - 展开 **Visibility, project features, permissions** 部分

2. **更改可见性级别**
   - 在 **Project visibility** 下选择 **Private**
   - 点击 **Save changes**

### 方法三：其他 Git 托管平台

不同平台的步骤可能略有不同，但通常都在：
- **Settings**（设置）→ **General**（常规）→ **Visibility**（可见性）
- 或类似的路径中

## 🔒 私有化后的安全检查清单

完成私有化后，请执行以下安全检查：

### 1. 检查敏感信息

```bash
# 搜索可能的密钥泄露
grep -r "sk-.*" --exclude-dir=.git
grep -r "api_key.*=.*[^your_]" --exclude-dir=.git
grep -r "private_key.*=.*[^your_]" --exclude-dir=.git
```

### 2. 确认 .gitignore 配置正确

确保以下文件已被忽略：
- `config.json` ✅（已在 .gitignore 中）
- `.env` ✅（已在 .gitignore 中）
- `decision_logs/` ✅（已在 .gitignore 中）

### 3. 检查 Git 历史

如果发现历史提交中有敏感信息：

```bash
# 查看提交历史
git log --all --full-history -- source

# 如果发现敏感信息，考虑使用 git-filter-repo 清理历史
# 注意：这会重写 Git 历史，需谨慎操作
```

### 4. 更新公开信息

私有化后，您可能需要：
- 更新 README 中的仓库链接（如果之前是公开链接）
- 移除或更新公开的示例截图中的敏感信息
- 更新文档中的引用链接

## 📝 推荐的后续操作

### 1. 添加安全策略

考虑创建 `.github/SECURITY.md` 文件：

```markdown
# 安全策略

## 支持的版本

当前支持的安全版本：
- v2.0.3 ✅

## 报告漏洞

如果您发现安全漏洞，请：
1. 不要公开披露
2. 通过 [联系方式] 私下报告
3. 我们将尽快响应和处理
```

### 2. 配置访问权限

- 在仓库设置中配置协作者权限
- 设置分支保护规则
- 启用必要的安全检查

### 3. 审查第三方集成

- 检查 CI/CD 配置（如 GitHub Actions）
- 审查第三方服务的访问权限
- 确认 webhook 和集成设置

## 🛡️ 长期安全建议

1. **定期审查访问权限**
   - 定期检查协作者列表
   - 移除不再需要的访问权限

2. **使用环境变量**
   - 所有敏感配置通过环境变量管理
   - 不要将真实密钥提交到代码库

3. **启用双重验证**
   - 对 Git 托管平台账户启用 2FA
   - 增强账户安全性

4. **定期更新依赖**
   - 保持依赖包最新
   - 及时修复安全漏洞

## ⚡ 快速检查命令

```bash
# 检查是否有真实的 API 密钥（非占位符）
echo "检查 config.json.example 中的示例地址..."
grep -E "(aster_user|aster_signer)" config.json.example

# 检查 .gitignore 是否配置正确
echo "检查 .gitignore..."
cat .gitignore | grep -E "(config\.json|\.env|decision_logs)"

# 检查是否有意外提交的敏感文件
echo "检查 Git 历史中的敏感文件..."
git log --all --full-history --oneline -- config.json .env
```

## 📞 获取帮助

如果在私有化过程中遇到问题：

1. 查看 Git 托管平台的官方文档
2. 联系平台支持团队
3. 检查仓库设置中的帮助文档

## ✅ 完成确认

私有化完成后，确认：

- [ ] 仓库已成功设为私有
- [ ] 未授权的用户无法访问
- [ ] 协作者权限已正确配置
- [ ] 敏感信息已检查并清理
- [ ] 文档已更新（如需要）
- [ ] 备份已完成

---

**最后更新**: 2025-01-XX

**注意**: 私有化后，仓库的公开链接将不再可访问。确保所有必要的协作者都已添加并具有适当的权限。
